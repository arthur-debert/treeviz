# Native 3viz format adapter
# This adapter preserves all fields from 3viz JSON format

# Extract fields from their exact names
label: label
type: type
children: children
icon: icon
content_lines: content_lines
source_location: source_location
extra: extra

# Use baseline icons from const.py
# (No custom icons needed - 3viz format includes icons in the data)